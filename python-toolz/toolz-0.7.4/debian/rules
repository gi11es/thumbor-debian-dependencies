#!/usr/bin/make -f
export PYBUILD_NAME=toolz
export PYBUILD_TEST_PYTEST=1
export PYBUILD_TEST_ARGS=toolz

%:
	dh $@ --with python2,python3 --buildsystem=pybuild

override_dh_clean:
	rm -rf toolz.egg-info .cache
	dh_clean
