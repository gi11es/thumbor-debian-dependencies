<?xml version="1.0" encoding="UTF\-8"?>
<!DOCTYPE html PUBLIC "\-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1\-transitional.dtd"><html xmlns="http://www.w3.org/1999/xhtml"><head><meta http\-equiv="Content\-Type" content="text/html; charset=UTF\-8" /><title>gif2rgb] [\-s 
<em class="replaceable"><code>width
<em class="replaceable"><code>heightVerbose mode (show progress). 
Enables printout of running scan lines.Only one file in the format of RGBRGB... triplets (Each of R,
G,B is a byte) is being read or written.  This file size is 3 * Width
* Height. If stdin is used for input or stdout for output, this option
is implicitly applied. The default (if not `\-1') is 3 files with the
names OutFileName.R, OutFileName.G, OutFileName.B, each of which is
Width * Height bytes. Specifies number of colors to use in RGB\-to\-GIF conversions, in
bits per pixels, so '\-c 8' actually specifies 256 colors (maximum and
default). Sets RGB\-to\-GIF conversion mode and specifies the size of the image 
to read. By default, convert a GIF input file to RGB triplets.  If \-s is 
specified, convert RGB input to a GIF.If no input file is given, gif2rgb will try to read adata
from stdin.