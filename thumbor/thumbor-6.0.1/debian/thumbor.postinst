#!/bin/sh

set -e

if [ "$1" = "configure" ] ; then
	if ! getent passwd thumbor | grep -q thumbor; then
		useradd --system --user-group thumbor
	fi

	chown -R thumbor:thumbor /usr/bin/thumbor*
fi

#DEBHELPER#

exit 0
