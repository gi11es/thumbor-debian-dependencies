#!/bin/sh

set -e

if [ "$1" = "configure" ] ; then
	if ! getent passwd resweb | grep -q resweb; then
		useradd --system --user-group --home /var/lib/resweb resweb
	fi

	chown -R resweb:resweb /usr/bin/resweb
fi

#DEBHELPER#

exit 0

